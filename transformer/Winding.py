import  transformers.models


class Winding:

    def __init__(self, voltage, current, current_density):
        self.voltage = voltage
        self.current = current
        self.current_density = current_density

    def calculate_wire_size(self)
        






